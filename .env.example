# Copy this file to .env for local development.
# In deployed environments these are injected via the ECS Task Definition
# (container environment variables), NOT via a checked-in .env file.

APP_NAME=production-grade-cicd
APP_ENV=local
APP_VERSION=1.0.0
LOG_LEVEL=INFO
PORT=8000
