fastapi==0.140.13
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.2.0
# starlette is a transitive dependency of fastapi (pulled in as 1.3.1 by the
# pin above). It's not listed directly here — see app/main.py's docstring
# note and README §6 for why FastAPI's own docs recommend NOT pinning
# starlette independently. This resolves well past 0.40.0, which fixes
# CVE-2024-47874 (starlette multipart/form-data DoS) flagged by Trivy.
